function separador(){
    var palavra = palavra.toLowerCase(document.getElementById("palavra"));
    var vg = document.getElementById('vg');
    var cs = document.getElementById('cs');
  
    for(var count=1; count<=10 ; count++)
    if(palavra=='a' || palavra=='b' || palavra=='c' || palavras=='d'){
        if(palavra=='a'){
            soma = soma + 1
        }
        if(palavra=='e'){
            soma = soma + 1
        }
        if(palavra=='i'){
            soma = soma + 1
        }
        if(palavra=='o'){
            soma = soma + 1
        }
        if(palavra=='u'){
            soma = soma + 1
        }
    }
    r.innerHTML = t

     if (num == 0 || isNaN(num)){
            alert("Informe um número válido")
            num.focus()
            return
        }
}